const devConfig = {
  // Uncomment the one you want to use
  // apiHost: 'localhost',
  apiHost: '192.168.100.23', // Your local IP
  backendPort: 5001,
  frontendPort: 8080
}

const config = devConfig

export default config // Default export
